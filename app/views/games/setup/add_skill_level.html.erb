<!-- <div class="container">
  <div class="row-vertical">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3"> -->

    <div class="relative-wizard">
      <div class="wizrd-card">
        <div class="wizard-card-header">
            <%= image_tag "mostplayed.png" %>
            <h2>Skill level</h2>
            <%= image_tag "mostplayed.png" %>
        </div>
          <div class="fucking-relatives">
          <%= simple_form_for @game, url: wizard_path, method: :put do |f| %>
            <%= f.input :skill_level, as: :hidden, input_html: { value: "1-3" } %>
            <%= f.button :submit, "ITN 1-3", class: "btn-sec-s width-button-card one" %>

          <% end %>

          <%= simple_form_for @game, url: wizard_path, method: :put do |f| %>
            <%= f.input :skill_level, as: :hidden, input_html: { value: "6-9" } %>
            <%= f.button :submit, "ITN 6-9", class: "btn-sec-s width-button-card two" %>
          <% end %>

          <%= simple_form_for @game, url: wizard_path, method: :put do |f| %>
            <%= f.input :skill_level, as: :hidden, input_html: { value: "3-6" } %>
            <%= f.button :submit, "ITN 3-6", class: "btn-sec-s width-button-card three" %>

          <% end %>

          <%= simple_form_for @game, url: wizard_path, method: :put do |f| %>
            <%= f.input :skill_level, as: :hidden, input_html: { value: "9+" } %>
            <%= f.button :submit, "ITN 9+", class: "btn-sec-s width-button-card four" %>
          <% end %>
        </div>

        <%= link_to "Back", previous_wizard_path %>
      </div>
    </div>
<!--     </div>
  </div>
</div> -->
